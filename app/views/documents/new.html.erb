<% content_for(:page_title) { t("documents.new", default: "Загрузить документ") } %>

<header class="page-header">
  <h1 class="title-1 m-0">📁 <%= t("documents.new", default: "Загрузить документ") %></h1>
  <p class="footnote text-secondary-apple m-0">
    <%= t("documents.new_hint", default: "После загрузки запустится автоматический разбор: paspol/snils/inn/договоры распознаются регулярными выражениями. AI используется только для длинных текстовых документов.") %>
  </p>
</header>

<%= render "form", document: @document %>
