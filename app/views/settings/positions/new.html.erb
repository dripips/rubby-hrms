<% content_for(:page_title) { t("settings.positions.new", default: "Новая должность") } %>

<header class="page-header">
  <div>
    <div class="page-header__eyebrow">💼 <%= t("settings.positions.title", default: "Должности") %></div>
    <h1 class="page-header__title m-0"><%= t("settings.positions.new", default: "Новая должность") %></h1>
  </div>
  <div class="page-header__actions">
    <%= link_to t("common.back"), settings_positions_path, class: "btn btn-ghost" %>
  </div>
</header>

<%= render "form", position: @position %>
