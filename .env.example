# Скопируй в `.env.development.local` и подставь свои значения.
# `.env.development.local` НЕ коммитится — он в .gitignore.

HRMS_DATABASE_HOST=localhost
HRMS_DATABASE_PORT=5432
HRMS_DATABASE_USERNAME=postgres
HRMS_DATABASE_PASSWORD=
